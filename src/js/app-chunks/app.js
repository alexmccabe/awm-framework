import bootstrap from '@/js/bootstrap.js';

/**
 * Immediately Invoked Function to launch the JS on this page. This isolates the
 * application from other JS that may be running on the page
 * @param  {object} root    The global root. Most often it is the Window object
 * @return {function}
 */
(function (root) {

    // App / Page logic goes here

} (window));